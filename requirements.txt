# Core runtime dependencies
requests>=2.32.0

# LLM Providers (Optionally used depending on LLM_PROVIDER env variable)
anthropic>=0.40.0
# openai>=1.50.0

# Local development utilities
python-dotenv>=1.0.0
pytest>=8.0.0
